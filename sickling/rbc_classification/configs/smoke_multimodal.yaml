# Tiny override for smoke-testing the multimodal classifier.
training:
  precision: 32-true
  batch_size: 32
  max_epochs: 2
  warmup_epochs: 0
  num_workers: 0

finetune:
  head_lr: 1.0e-3
  backbone_lr: 1.0e-4
  minority_frac: 0.5
